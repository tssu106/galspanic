# Gals Panic — 진행 현황

> 원작 아케이드 **갈스패닉(Qix 계열)** 웹 이식. 마지막 업데이트: 2026-08-16

---

## 한눈에 보기

| 영역 | 상태 | 비고 |
|------|------|------|
| 로컬 동일화면 co-op (`client/local.html`) | ✅ 완성 | 서버 불필요, 바로 실행 |
| 온라인 서버 권위 모드 (`server/` + `client/index.html`) | ✅ 동작 | Colyseus, 델타 동기화 |
| Qix flood-fill 점유 메커니즘 | ✅ | 적을 가둘수록 크게 먹음 |
| 레벨 스케일링(적 수·속도) | ✅ | 공개율에 따라 적 추가 |
| **몬스터 다양화** | ✅ **신규** | 5종 아키타입 + 고유 AI/외형 |
| **몬스터 포획(트랩)** | ✅ **신규** | 좁은 곳에 가두면 적 제거 + 보너스 점수 |
| **선 긋기 중 멈춤 = 되짚기** | ✅ **신규** | 파먹는 도중 멈추면 그린 선을 따라 되돌아감 |
| **이미지 4배 확대** | ✅ **신규** | 800×600 → 1600×1200 |
| 창 포커스 상실 시 방향 고착 버그 | ✅ 수정됨 | `blur`/`visibilitychange`에서 키 해제 |

---

## 게임 규칙

1. 안전지대(칠해진 곳)에서 빈 공간으로 들어가면 **선(trail)** 이 그려진다.
2. 선이 다시 안전지대에 닿으면, 적이 닿을 수 없는 빈 영역이 **한 번에 점유**(flood-fill)되어 숨은 그림이 드러난다.
3. 돌아다니는 **적**이 당신의 선에 닿으면 그 선의 주인이 사망(목숨 −1, 부활).
4. 내부의 **80%** 를 열면 클리어. 점유 면적으로 기여도/순위 산정.

**조작 (로컬)** — P1 방향키 · P2 `WASD` · P3 `IJKL` · P4 숫자패드 `8/5/4/6` · `P` 일시정지 · `R` 재시작

---

## 이번 업데이트 상세

### 1) 몬스터 다양화 (5종 아키타입)

각 적은 고유한 **외형**, **속도**, **행동 AI** 를 가지며, 레벨이 오를수록 위험한 변종이 등장한다.
등장 확률은 `weight` 로 가중치 부여.

| 이름 | 외형 | 속도 | 행동 | 등장 레벨 |
|------|------|------|------|-----------|
| 별똥이 (star) | 빨간 4각 별 | 1.0× | `bounce` 직선 반사 | 1+ |
| 톱니 (saw) | 주황 톱니바퀴 | 1.15× | `bounce` 직선 반사 | 1+ |
| 슬라임 (blob) | 보라 젤리(꿈틀) | 0.7× | `wander` 무작위 방향전환 | 2+ |
| 유령 (ghost) | 반투명 유령 | 0.9× | `wander` 무작위 방향전환 | 2+ |
| 추적자 (dart) | 청록 삼각 화살 | 1.55× | `hunt` 플레이어 추적 | 3+ |

- **bounce**: 원작 Qix처럼 벽에 튕기며 직진.
- **wander**: 0.6~1.8초마다 진행 방향을 무작위로 틀어 예측 불가.
- **hunt**: 가장 가까운 플레이어(특히 선을 긋는 중인 플레이어)를 향해 약하게 조준 → 선 긋는 동안 압박.
- 이동 속도 크기는 항상 보존되고 **방향만** 바뀌어, 조준/방황이 자연스럽다.

### 2) 몬스터 포획 (트랩 메커니즘)

원작 Qix처럼 **적을 좁은 곳에 몰아넣는** 플레이에 보상을 준다.

- 선을 이어 **적을 작은 빈 영역(내부 면적의 10% 이하)에 완전히 가두면**:
  - 그 구역을 즉시 점유하고,
  - 갇힌 적을 **제거**하며,
  - 가둔 플레이어에게 **포획 보너스 +400점** (마리당) 지급.
- 여러 마리를 한 번에 가두면 모두 잡히고 보너스도 합산.
- 구현: 점유 시점에 남은 빈칸을 **연결 요소(connected component)** 로 분해 →
  적 없는 폭은 기존처럼 점유, 적이 든 **작은** 폭은 트랩으로 처리, 적이 든 **넓은** 영역은 그대로 개방.
- 피드백: 포획 지점에 `포획! +400` 플로팅 팝업 + 밝은 효과음, HUD/결과창에 `🕸` 포획 수 표시.
- 순위는 이제 **점수(점유 면적 + 포획 보너스)** 순으로 산정.
- 밸런스 상수: `TRAP_RATIO=0.10`, `TRAP_BONUS=400` (튜닝 가능).

### 3) 선 긋기 중 멈춤 = 되짚기 (커밋 위험 부담)

빈 공간을 파먹는 도중에는 **계속 움직여야** 한다. 긴장감을 위해:

- 선을 긋는 중(가장자리/안전지대 밖) **키 입력이 멈추면** `STALL_MS=400ms` 후:
  - 지금까지 **그린 선을 따라 역순으로 되짚어 걸어서** 시작한 안전지대 지점까지 돌아간다.
  - 되짚는 동안 선은 밟은 만큼 지워지고(점유 안 함), 완전히 돌아오면 조작이 풀린다.
- 목숨은 잃지 않는다 — 단순히 그리던 선을 무르는 것.
- 되짚는 중에는 조작 불가(끝까지 자동으로 되돌아감). 남은 선은 여전히 적에게 닿으면 사망하므로 위험 부담이 있다.
- 방향 전환 사이의 짧은 공백은 400ms 여유로 흡수(오작동 방지).
- 피드백: `취소` 팝업 + 하강 효과음.
- 구현: 프레임 루프에서 `p.drawing && 방향 없음` 시간을 누적 → `startRetreat(p)` →
  이동 타이머마다 `retreatStep(p)` 로 trail 끝 셀부터 한 칸씩 되밟아 삭제.

### 4) 이미지 4배 확대

- 셀 크기 `CELL 5 → 10` (그리드 해상도 160×120은 유지) → 캔버스 **1600×1200** (면적 4배).
- 절차적 아트가 2배 해상도로 그려져 더 선명하다.
- `max-width:100%` 로 뷰포트를 넘지 않게 축소 렌더 → 어떤 화면에서도 가로 스크롤 없이 크게 표시.

---

## 아키텍처

```
client/
  local.html    로컬 멀티 (엔진+렌더+입력 all-in-one, 서버 불필요)
  index.html    온라인 클라이언트 (입력만 전송, 동기화 상태 렌더)
  status.html   ← 이 진행 현황 뷰 페이지 (신규)
server/src/
  GalSim.ts     서버 권위 Qix 시뮬레이션 (DOM-free, local 엔진의 포팅)
  GameRoom.ts   ~30Hz 틱 루프 → Colyseus 스키마 델타 동기화
  schema.ts     동기화 상태(grid/trail/players/enemies/phase/level)
  constants.ts  그리드/보더/속도/목표치
```

**온라인 설계 포인트**
- **서버 권위**: 클라는 방향 코드(0~4)만 전송, 서버 `GalSim` 이 이동·충돌·점유 계산 → 치트 방지.
- **이미지 보안**: 원본 풀해상도 미전송, 흐린 버전만 배포하고 열린 셀만 선명하게.

---

## 남은 로드맵

1. **온라인 엔진(`GalSim.ts`)에 5종 몬스터·이미지 확대 동기화** — 현재 신규 변경은 `local.html` 에만 반영됨.
2. 서버 타일 스트리밍으로 이미지 완전 보호.
3. 라인 위를 달리는 보조 적(Sparx) 추가로 원작 난이도 재현.
4. 클리어 후 "이 그림 뭐게?" 맞히기 보너스.
5. 로비/방 코드, 관전자, 8인 이상 확장(보더 스폰 분산).
